apiVersion: v1
kind: Secret
metadata:
  name: webapp-secret
  namespace: devsecops
  labels:
    app: webapp
type: Opaque
stringData:
  # Use example values - the app will use PostgreSQL instead
  SUPABASE_URL: "https://example.supabase.co"
  SUPABASE_ANON_KEY: "example-anon-key"
